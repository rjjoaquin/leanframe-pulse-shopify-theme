{% doc %}
  @description Badge typically placed on top of card elements

  @param {string} text - text for the badge
{% enddoc %}
<div class="absolute px-4 py-2 max-w-[10rem] bg-text text-background left-3 top-3">{{ text }}</div>
